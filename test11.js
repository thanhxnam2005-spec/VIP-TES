import { TimotxtAdapter } from './lib/scraper/adapters/Timotxt.ts';
import fs from 'fs';

const test = async () => {
    // Fake extensionFetch by overriding import
    // But this is TS. Let's just test the /dir fetching manually.
};
